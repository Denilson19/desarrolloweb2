/*function printColor() {
    // Solicitar al usuario que ingrese el nombre del color
    var colorName = prompt("Por favor, ingresa el nombre del color:");
  
    // Obtener el código hexadecimal del nombre del color
    var colorCode = getColorCode(colorName);
  
    // Validar si el color es válido
    if (colorCode) {
      // Imprimir en la página web
      document.body.style.backgroundColor = colorCode;
  
      // Imprimir en la consola del navegador
      console.log('Color: ' + colorName + ' (' + colorCode + ')');
    } else {
      alert("El color ingresado no es válido. Por favor, ingresa un color válido.");
    }
  }
  
  // Función para obtener el código hexadecimal de un nombre de color
  function getColorCode(colorName) {
    switch (colorName.toLowerCase()) {
      case 'rojo':
        return '#ff0000';
      case 'verde':
        return '#00ff00';
      case 'azul':
        return '#0000ff';
      // Agrega más casos según los colores que desees admitir
      default:
        return null;
    }
  }
  
  // Obtener el botón y agregar un evento de clic
  document.getElementById('colorButton').addEventListener('click', printColor);*/
  /*
Descripcion: Ejemplo de una suma
Desarollador: Denilson Noel Ledezma Condori
Fecha: 2024-04-15
Cambios: NInguno
*/ 
  var color= prompt("Ingrese un color");
  console.log("color Ingresa es ",color);

  